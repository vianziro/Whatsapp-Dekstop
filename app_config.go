package main

// Public application metadata. These values are needed by every clean build;
// they are intentionally not credentials or release secrets.
const (
	windowTitle = "WhatsApp Desk"
	appURL      = "https://web.whatsapp.com"
)
